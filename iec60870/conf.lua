-- protocol configuration
--

local conf = {
	ADDR_LEN		= 2, -- Address size
	FT12_FIXED_LEN	= 3, -- FIXED Frame FT1.2 length
	ASDU_COT_SIZE	= 1, -- cause of transfer
	ASDU_CAOA_SIZE	= 1, -- common address of ASDU
}

return conf
